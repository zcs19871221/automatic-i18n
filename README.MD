# automatic-i18n

[![CI](./assets/badge.svg)](https://github.com/zcs19871221/automatic-i18n/actions/workflows/npm-publish.yml)
[![Test coverage](https://img.shields.io/coverallsCoverage/github/zcs19871221/automatic-i18n)](https://coveralls.io/github/zcs19871221/automatic-i18n)
[![npm version](https://img.shields.io/npm/v/automatic-i18n?style=flat-square)](https://www.npmjs.com/package/automatic-i18n)

[English](./README.MD) | [简体中文](./README.zh-CN.md)

Automatically extract and replace hard-coded locale text in TypeScript, JavaScript, and React code with one command.

`automatic-i18n` scans source files, parses AST, replaces matched text with i18n calls, and generates locale files.

## Why automatic-i18n

- No large manual refactor before i18n adoption.
- Supports JSX text, string literals, and template strings with variables.
- Works with `react-intl` by default.
- Supports hook mode and global mode.
- Supports custom formatter implementation via `I18nFormatter`.
- Includes a merge helper for locale files with git conflict markers.

## Quick Start (60 seconds)

1. Install required packages in your project:

```bash
npm i -D automatic-i18n typescript prettier fs-extra
```

2. Run the replacer on your project:

```bash
npx automatic-i18n -t src
```

3. Check generated files under `./i18n` (default).

For command help:

```bash
npx automatic-i18n -h
```

## CLI

The package provides a CLI through `npx automatic-i18n` (or `npx i18n` after install).

### Common examples

Replace Chinese text from `src` and generate both `zh-cn` and `en-us` locale files:

```bash
npx automatic-i18n -t src -sl zh-cn -tl zh-cn en-us
```

Use global intl object mode instead of hook mode:

```bash
npx automatic-i18n -t src -g
```

Exclude specific folders:

```bash
npx automatic-i18n -t src -e dist build
```

Enable stable hash-like keys from message text:

```bash
npx automatic-i18n -t src -u
```

Merge locale files that contain git conflict markers:

```bash
npx automatic-i18n merge ./i18n ./i18n
```

### CLI options

- `-t, --targets <fileOrDir...>`: files or directories to process. Default: current working directory.
- `-d, --distLocaleDir <fileOrDir>`: output directory of locale files. Default: `<cwd>/i18n`.
- `-sl, --localeToReplace <locale>`: source locale to detect in code. Choices: `en-us`, `zh-cn`. Default: `zh-cn`.
- `-tl, --localesToGenerate <locales...>`: locales to generate. Default: `zh-cn en-us`.
- `-g, --global`: use global intl object mode.
- `-e, --excludes <filesOrDirs...>`: names to exclude.
- `-m, --meaningKey`: convert auto-generated keys to meaningful English-style keys when possible.
- `-u, --uniqIntlKey`: generate keys from message hash to reduce key conflicts.
- `--englishStrategy <mode>`: english extraction strategy for `localeToReplace=en-us`. Choices: `comment-only`, `balanced`, `aggressive`. Default: `comment-only`.
- `-db, --debug`: print debug logs.
- `-v, --version`: show version.

## Before and After

Before:

```tsx
import React from 'react';

function Component() {
  const en = 'English';
  const cn = 'Chinese';
  const locales = `${en} and ${cn}`;

  return (
    <div>
      Please choose your locale from: {en} {cn}
    </div>
  );
}
```

After:

```tsx
import { useIntl, FormattedMessage } from 'react-intl';
import React from 'react';

function Component() {
  const intl = useIntl();

  const en = intl.formatMessage({
    id: 'key0001',
    defaultMessage: 'English',
  });
  const cn = intl.formatMessage({
    id: 'key0002',
    defaultMessage: 'Chinese',
  });
  const locales = intl.formatMessage({
    id: 'key0003',
    defaultMessage: '{v1} and {v2}',
    values: { v1: en, v2: cn },
  });

  return (
    <div>
      <FormattedMessage
        id="key0004"
        defaultMessage="Please choose your locale from: {v1} {v2}"
        values={{ v1: en, v2: cn }}
      />
    </div>
  );
}
```

Generated locale file example:

```ts
/*
 * This file is automatically generated by automatic-i18n.
 * You should only change locale values.
 */
import { LocalKey } from './types';

const locale: Record<LocalKey, string> = {
  key0001: 'English',
  key0002: 'Chinese',
  key0003: '{v1} and {v2}',
  key0004: 'Please choose your locale from: {v1} {v2}',
};

export default locale;
```

Live demo project:

https://github.com/zcs19871221/local-development-console

Preview screenshot:

![](./image.png)

## API

```js
const I18nReplacer = require('automatic-i18n').default;

I18nReplacer.createI18nReplacer({
  targets: ['src'],
  uniqIntlKey: true,
}).replace();
```

```ts
export interface ReplacerOpt {
  targets?: string[];
  distLocaleDir?: string;
  localeToReplace?: 'en-us' | 'zh-cn';
  localesToGenerate?: Array<'en-us' | 'zh-cn'>;
  global?: boolean;
  meaningKey?: boolean;
  uniqIntlKey?: boolean;
  I18nFormatter?: I18nFormatterCtr;
  filters?: Filter[];
  excludes?: string[];
  debug?: boolean;
  englishStrategy?: 'comment-only' | 'balanced' | 'aggressive';
  outputToNewDir?: string;
  addMissingDefaultMessage?: boolean;
}
```

Also exported:

- `resolveMergeConflict(distDir, outputDir)`

## Safety Recommendations

For large repositories, start with a dry rollout pattern:

1. Commit current code first.
2. Compare diffs and run tests.
3. Merge after review.

## Comment Controls

### Ignore next line

```ts
/* auto-i18n-ignore-next */
const name = 'chengsiZhang';
```

### Ignore a code block

```ts
/* auto-i18n-ignore-start */
const name = 'chengsiZhang';
/* auto-i18n-ignore-end */
```

### English source mode (`localeToReplace: 'en-us'`)

In English mode, plain string literals are not collected unless you explicitly mark them.

Use one of:

- `/* auto-i18n-collect-start */`
- `/* auto-i18n-collect-end */`
- `/* auto-i18n-collect-next */`

#### How `englishStrategy` works

Detailed decision document: [docs/english-strategy.md](./docs/english-strategy.md)

`englishStrategy` controls automatic extraction when `localeToReplace` is `en-us`:

- `comment-only`: safest mode. String literals require collect comments.
- `balanced`: extracts sentence-like literals and keeps key-like literals unchanged.
- `aggressive`: extracts most literals except hard blacklisted contexts.

Decision order for English extraction:

1. If collect comment matches, extract directly.
2. If node is in blacklist context, never extract.
3. Otherwise apply strategy behavior.

Current blacklist examples include import paths, object property names, element access keys (`obj['key']`), and technical JSX attributes like `className`, `id`, `data-testid`, `href`, and `src`.

Example:

```tsx
// before
function Component(fetchedData: any) {
  /* auto-i18n-collect-next */
  const title = 'welcome';
  const key = 'user';

  return <Header title={title}>Nice to meet you: {fetchedData[key]}</Header>;
}

// after
function Component(fetchedData: any) {
  /* auto-i18n-collect-next */
  const title = i18n.intl.formatMessage({
    id: 'key1fa62a482__',
    defaultMessage: 'welcome',
  });
  const key = 'user';

  return (
    <Header title={title}>
      {i18n.intl.formatMessage(
        {
          id: 'key1585b57b2__',
          defaultMessage: 'Nice to meet you: {v1}',
        },
        { v1: fetchedData[key] }
      )}
    </Header>
  );
}
```

## Limitations

- This tool focuses on TypeScript/JavaScript/JSX/TSX source parsing.
- English mode requires explicit collection comments for string literals.
- Always review generated diffs before merging in production branches.

## License

MIT
