const name = 'name';
const age = 'name';
const rule = 'rule';
const what = 'what';
const end = 'end';

const x = `fsffdsfd${rule}你好呀${name}哈哈${age ? `里面${what}` : end}ffff${
  name ? 'sdffds' : 'fdsfdf'
}${age > name ? '里面' : '外面'}`;

export { x };
